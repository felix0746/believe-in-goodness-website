# 相信善良網站 - 程式使用手冊

## 📋 目錄

1. [專案概述](#專案概述)
2. [技術架構](#技術架構)
3. [專案結構](#專案結構)
4. [環境設置](#環境設置)
5. [開發指南](#開發指南)
6. [組件說明](#組件說明)
7. [頁面說明](#頁面說明)
8. [功能配置](#功能配置)
9. [部署流程](#部署流程)
10. [維護指南](#維護指南)
11. [常見問題](#常見問題)

---

## 專案概述

「相信善良」是一個非營利組織的官方網站，致力於為弱勢群體（包含外籍生、偏鄉孩童、更生人）提供教育與就業機會，培養餐旅菁英。

### 主要功能
- 🌐 多語言支援（繁體中文、英文、越南文）
- 📱 響應式設計（優先考慮移動端體驗）
- 🖼️ 圖片優化與 WebP 格式支援
- 📝 聯繫表單整合 Google Forms
- 🔍 SEO 優化（Sitemap、Robots.txt、Meta 標籤）
- 🔥 Firebase 整合（數據存儲）

---

## 技術架構

### 核心技術棧

| 技術 | 版本 | 用途 |
|------|------|------|
| **Next.js** | 15.5.0 | React 框架，支援 App Router |
| **React** | 19.1.0 | UI 框架 |
| **TypeScript** | ^5 | 類型安全 |
| **Tailwind CSS** | ^4 | 樣式框架 |
| **Firebase** | ^12.1.0 | 後端服務（Firestore） |
| **i18next** | ^25.4.2 | 國際化框架 |

### 開發工具

- **Turbopack**: Next.js 的新建構工具，提供更快的開發體驗
- **ESLint**: 代碼品質檢查
- **PostCSS**: CSS 處理
- **Sharp**: 圖片處理優化

---

## 專案結構

```
相信善良網站/
├── public/                    # 靜態資源
│   └── images/               # 圖片資源
│       ├── *.png            # 原始圖片
│       └── webp/            # WebP 格式圖片
│
├── src/
│   ├── app/                  # Next.js App Router 頁面
│   │   ├── layout.tsx       # 根佈局
│   │   ├── page.tsx         # 首頁
│   │   ├── globals.css      # 全局樣式
│   │   ├── sitemap.ts       # 網站地圖生成
│   │   ├── robots.ts        # 搜尋引擎爬蟲配置
│   │   │
│   │   ├── story/           # 善良故事頁面
│   │   │   ├── page.tsx
│   │   │   ├── StoryTabs.tsx
│   │   │   ├── KindnessMessages.tsx
│   │   │   ├── NewsUpdates.tsx
│   │   │   └── MilestonesTimeline.tsx
│   │   │
│   │   ├── impact/          # 影響力頁面
│   │   │   ├── page.tsx
│   │   │   └── ImpactPageClient.tsx
│   │   │
│   │   ├── care/            # 關懷與溫暖頁面
│   │   │   ├── page.tsx
│   │   │   └── CareTabs.tsx
│   │   │
│   │   ├── contact/         # 聯繫我們頁面
│   │   │   ├── page.tsx
│   │   │   └── ContactPageClient.tsx
│   │   │
│   │   ├── faq/             # 常見問題頁面
│   │   ├── privacy-policy/  # 隱私權政策
│   │   └── terms-of-use/    # 服務條款
│   │
│   ├── components/          # 可重用組件
│   │   ├── Navbar.tsx       # 導航欄
│   │   ├── Footer.tsx       # 頁尾
│   │   ├── OptimizedImage.tsx  # 優化圖片組件
│   │   ├── ContactFormModal.tsx # 聯繫表單模態框
│   │   ├── LanguageSwitcher.tsx # 語言切換器
│   │   └── AnimatedNumber.tsx   # 動畫數字組件
│   │
│   ├── hooks/               # 自定義 Hooks
│   │   └── useTranslation.tsx  # 翻譯 Hook
│   │
│   ├── firebase.ts          # Firebase 配置
│   └── languages.json       # 多語言翻譯文件
│
├── next.config.ts           # Next.js 配置
├── tsconfig.json           # TypeScript 配置
├── postcss.config.mjs      # PostCSS 配置
├── eslint.config.mjs       # ESLint 配置
└── package.json            # 專案依賴
```

---

## 環境設置

### 前置需求

- **Node.js**: 建議使用 18.x 或更高版本
- **npm** 或 **yarn**: 套件管理工具
- **Git**: 版本控制

### 安裝步驟

1. **克隆專案**
```bash
git clone https://github.com/felix0746/believe-in-goodness-website.git
cd believe-in-goodness-website
```

2. **安裝依賴**
```bash
npm install
```

3. **設置環境變數**

創建 `.env.local` 文件（如果尚未存在）：

```env
# Firebase 配置
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_auth_domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_storage_bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
```

4. **啟動開發伺服器**
```bash
npm run dev
```

網站將在 `http://localhost:3000` 運行。

### 可用指令

| 指令 | 說明 |
|------|------|
| `npm run dev` | 啟動開發伺服器（使用 Turbopack） |
| `npm run build` | 建置生產版本 |
| `npm run start` | 啟動生產伺服器 |
| `npm run lint` | 執行 ESLint 檢查 |

---

## 開發指南

### 添加新頁面

1. 在 `src/app/` 目錄下創建新資料夾
2. 在資料夾內創建 `page.tsx` 文件
3. 導出 React 組件作為頁面內容

**範例：**
```typescript
// src/app/about/page.tsx
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '關於我們｜相信善良',
  description: '了解相信善良的使命與願景',
};

export default function AboutPage() {
  return (
    <main>
      <h1>關於我們</h1>
      {/* 頁面內容 */}
    </main>
  );
}
```

### 添加新組件

1. 在 `src/components/` 目錄下創建組件文件
2. 使用 TypeScript 定義 Props 介面
3. 導出組件

**範例：**
```typescript
// src/components/MyComponent.tsx
'use client';

interface MyComponentProps {
  title: string;
  description?: string;
}

export default function MyComponent({ title, description }: MyComponentProps) {
  return (
    <div>
      <h2>{title}</h2>
      {description && <p>{description}</p>}
    </div>
  );
}
```

### 使用多語言功能

1. **在 `languages.json` 中添加翻譯鍵值**

```json
{
  "zh-Hant": {
    "myNewKey": "我的新文字"
  },
  "en": {
    "myNewKey": "My New Text"
  }
}
```

2. **在組件中使用翻譯**

```typescript
'use client';

import { useTranslation } from '@/hooks/useTranslation';

export default function MyComponent() {
  const { t } = useTranslation();
  
  return <h1>{t('myNewKey')}</h1>;
}
```

### 圖片使用最佳實踐

1. **使用 OptimizedImage 組件**

```typescript
import OptimizedImage from '@/components/OptimizedImage';

<OptimizedImage
  src="/images/example.png"
  alt="描述文字"
  width={500}
  height={300}
  priority={false}  // 首屏圖片設為 true
/>
```

2. **圖片路徑規則**
   - 原始圖片放在 `public/images/`
   - WebP 版本放在 `public/images/webp/`
   - 組件會自動嘗試載入 WebP，失敗時回退到原始格式

---

## 組件說明

### Navbar（導航欄）

**位置**: `src/components/Navbar.tsx`

**功能**:
- 響應式導航選單
- 滾動時樣式變化
- 移動端漢堡選單
- 語言切換整合

**使用方式**:
已在 `layout.tsx` 中全局引入，無需手動添加。

### Footer（頁尾）

**位置**: `src/components/Footer.tsx`

**功能**:
- 三欄式佈局
- 社交媒體連結
- 聯絡資訊
- 版權宣告

**自定義**:
修改 `Footer.tsx` 或更新 `languages.json` 中的翻譯鍵值。

### OptimizedImage（優化圖片）

**位置**: `src/components/OptimizedImage.tsx`

**Props**:
```typescript
interface OptimizedImageProps {
  src: string;           // 圖片路徑（相對於 public）
  alt: string;          // 替代文字
  width: number;         // 寬度
  height: number;       // 高度
  priority?: boolean;    // 是否優先載入
  className?: string;   // CSS 類名
  style?: React.CSSProperties;  // 內聯樣式
}
```

**特性**:
- 自動嘗試載入 WebP 格式
- 載入失敗時回退到原始格式
- 使用 Next.js Image 組件優化

### ContactFormModal（聯繫表單）

**位置**: `src/components/ContactFormModal.tsx`

**功能**:
- 整合 Google Forms
- 表單驗證
- 提交狀態管理
- 成功/錯誤提示

**配置 Google Forms**:

1. 在 `ContactFormModal.tsx` 中找到以下區塊：
```typescript
const actionUrl = 'YOUR_GOOGLE_FORM_URL';
const nameEntryId = 'entry.XXXXX';      // 姓名欄位 ID
const phoneEntryId = 'entry.XXXXX';     // 電話欄位 ID
const emailEntryId = 'entry.XXXXX';     // Email 欄位 ID
const messageEntryId = 'entry.XXXXX';   // 訊息欄位 ID
```

2. 獲取 Google Form 欄位 ID：
   - 打開 Google Form
   - 右鍵點擊欄位 → 檢查元素
   - 在 HTML 中找到 `name="entry.XXXXX"`

### AnimatedNumber（動畫數字）

**位置**: `src/components/AnimatedNumber.tsx`

**功能**:
- 數字動畫效果
- 用於統計數據展示

**使用方式**:
```typescript
<AnimatedNumber end={1000} duration={2000} />
```

**Props**:
- `end`: 目標數字
- `duration`: 動畫持續時間（毫秒，預設 2000）

### LanguageSwitcher（語言切換器）

**位置**: `src/components/LanguageSwitcher.tsx`

**功能**:
- 語言切換下拉選單
- 保存用戶語言偏好到 localStorage

**支援語言**:
- 繁體中文 (zh-Hant)
- 英文 (en)
- 越南文 (vi)

---

## 頁面說明

### 首頁 (`/`)

**文件**: `src/app/page.tsx`

**內容區塊**:
1. 背景緣由
2. 我們的承諾
3. 信念與期望

**自定義**:
修改 `page.tsx` 或更新 `languages.json` 中的 `home*` 鍵值。

### 善良故事 (`/story`)

**文件**: `src/app/story/`

**功能**:
- 標籤頁切換（善良訊息、善良快訊、里程碑）
- 動態內容展示

**組件**:
- `StoryTabs.tsx`: 主容器
- `KindnessMessages.tsx`: 善良訊息
- `NewsUpdates.tsx`: 善良快訊
- `MilestonesTimeline.tsx`: 里程碑時間軸

### 我們的影響力 (`/impact`)

**文件**: `src/app/impact/`

**功能**:
- 問題展示
- 目標統計（使用 AnimatedNumber）
- 數據視覺化

**自定義統計數據**:
修改 `ImpactPageClient.tsx` 中的數字和單位。

### 關懷與溫暖 (`/care`)

**文件**: `src/app/care/`

**功能**:
- 國內/國外標籤切換
- 桌面版卡片佈局
- 移動版手風琴式佈局

**內容配置**:
在 `CareTabs.tsx` 中修改 `domesticContent` 和 `internationalContent` 陣列。

**添加新項目**:
```typescript
{
  titleKey: 'yourTitleKey',
  textKey: 'yourTextKey',
  image: '/images/your-image.png',
  alt: '圖片描述',
}
```

### 聯繫我們 (`/contact`)

**文件**: `src/app/contact/`

**功能**:
- 服務介紹
- 聯繫表單
- 地址與聯絡資訊
- 地圖整合

**自定義**:
- 修改 `ContactPageClient.tsx` 中的內容
- 更新 `languages.json` 中的 `contact*` 鍵值

### 其他頁面

- **常見問題** (`/faq`): `src/app/faq/page.tsx`
- **隱私權政策** (`/privacy-policy`): `src/app/privacy-policy/page.tsx`
- **服務條款** (`/terms-of-use`): `src/app/terms-of-use/page.tsx`

---

## 功能配置

### Firebase 配置

**文件**: `src/firebase.ts`

**設置步驟**:
1. 在 Firebase Console 創建專案
2. 啟用 Firestore Database
3. 獲取配置資訊
4. 在 `.env.local` 中設置環境變數
5. 在 `firebase.ts` 中配置（已自動讀取環境變數）

**使用範例**:
```typescript
import { db } from '@/firebase';
import { collection, getDocs } from 'firebase/firestore';

// 讀取數據
const querySnapshot = await getDocs(collection(db, 'your-collection'));
```

### SEO 配置

#### Sitemap

**文件**: `src/app/sitemap.ts`

**更新方式**:
```typescript
export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: 'https://your-domain.com/your-page',
      lastModified: new Date(),
      changeFrequency: 'monthly',
      priority: 0.8,
    },
  ];
}
```

#### Robots.txt

**文件**: `src/app/robots.ts`

**配置**:
```typescript
export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/admin/',  // 禁止爬取的路徑
    },
    sitemap: 'https://your-domain.com/sitemap.xml',
  };
}
```

#### Meta 標籤

每個頁面的 `page.tsx` 中都有 `metadata` 導出：

```typescript
export const metadata: Metadata = {
  title: '頁面標題',
  description: '頁面描述',
  openGraph: {
    title: '分享標題',
    description: '分享描述',
    images: [{ url: '分享圖片URL' }],
  },
};
```

### 圖片優化配置

**文件**: `next.config.ts`

**當前配置**:
```typescript
images: {
  formats: ['image/webp', 'image/avif'],
  deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
  imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
}
```

---

## 部署流程

### Vercel 部署（推薦）

本專案已配置 GitHub 整合，推送到 GitHub 會自動觸發部署。

**手動部署步驟**:

1. **安裝 Vercel CLI**
```bash
npm i -g vercel
```

2. **登入 Vercel**
```bash
vercel login
```

3. **部署**
```bash
vercel
```

4. **生產環境部署**
```bash
vercel --prod
```

### 環境變數設置

在 Vercel 專案設置中添加環境變數：
- `NEXT_PUBLIC_FIREBASE_API_KEY`
- `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`
- `NEXT_PUBLIC_FIREBASE_PROJECT_ID`
- `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`
- `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`
- `NEXT_PUBLIC_FIREBASE_APP_ID`

### 建置檢查

部署前確保：
- ✅ 所有環境變數已設置
- ✅ `npm run build` 成功執行
- ✅ 沒有 ESLint 錯誤
- ✅ 所有圖片路徑正確

---

## 維護指南

### 更新內容

#### 更新文字內容

1. 打開 `src/languages.json`
2. 找到對應的語言和鍵值
3. 修改文字內容
4. 提交更改

#### 更新圖片

1. **添加新圖片**:
   - 將原始圖片放入 `public/images/`
   - 轉換為 WebP 格式放入 `public/images/webp/`
   - 在組件中更新圖片路徑

2. **更換現有圖片**:
   - 替換 `public/images/` 中的文件（保持相同檔名）
   - 替換 `public/images/webp/` 中的 WebP 文件
   - 清除瀏覽器快取或重新部署

#### 更新聯絡資訊

1. **頁尾聯絡資訊**: 修改 `src/components/Footer.tsx`
2. **聯繫頁面**: 修改 `src/app/contact/ContactPageClient.tsx`
3. **翻譯文件**: 更新 `languages.json` 中的相關鍵值

### 添加新語言

1. **在 `languages.json` 中添加新語言物件**
```json
{
  "zh-Hant": { ... },
  "en": { ... },
  "vi": { ... },
  "ja": {  // 新語言
    "siteTitle": "善良を信じる",
    // ... 其他翻譯
  }
}
```

2. **在 `LanguageSwitcher.tsx` 中添加選項**
```typescript
const languages = [
  { code: 'zh-Hant', name: '繁體中文' },
  { code: 'en', name: 'English' },
  { code: 'vi', name: 'Tiếng Việt' },
  { code: 'ja', name: '日本語' },  // 新增
];
```

### 性能優化

1. **圖片優化**:
   - 使用 WebP 格式
   - 適當的圖片尺寸
   - 使用 `priority` 屬性標記首屏圖片

2. **代碼分割**:
   - Next.js 自動進行代碼分割
   - 使用動態導入減少初始載入：
```typescript
const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
  loading: () => <p>載入中...</p>,
});
```

3. **快取策略**:
   - 靜態頁面自動快取
   - 使用 `revalidate` 設置重新驗證時間

### 錯誤處理

1. **檢查控制台**: 瀏覽器開發者工具
2. **檢查建置日誌**: Vercel 部署日誌
3. **檢查環境變數**: 確保所有變數已正確設置

---

## 常見問題

### Q: 如何修改網站標題和描述？

**A**: 修改 `src/app/layout.tsx` 中的 `metadata` 物件。

### Q: 圖片無法顯示？

**A**: 檢查：
1. 圖片路徑是否正確（相對於 `public` 目錄）
2. 圖片文件是否存在
3. WebP 版本是否存在（如果使用 OptimizedImage）

### Q: 語言切換不生效？

**A**: 檢查：
1. `languages.json` 中是否有對應的翻譯鍵值
2. 瀏覽器 localStorage 是否被清除
3. 組件是否正確使用 `useTranslation` hook

### Q: 表單提交失敗？

**A**: 檢查：
1. Google Form 的 URL 是否正確
2. 欄位 ID 是否正確
3. 表單是否允許匿名提交

### Q: 建置失敗？

**A**: 檢查：
1. 所有依賴是否已安裝：`npm install`
2. TypeScript 錯誤：`npm run build` 查看詳細錯誤
3. ESLint 錯誤：`npm run lint`

### Q: 如何添加新的頁面路由？

**A**: 在 `src/app/` 下創建新資料夾，添加 `page.tsx` 文件即可。Next.js 會自動生成路由。

### Q: 樣式沒有生效？

**A**: 檢查：
1. CSS 類名是否正確
2. Tailwind 類名是否在配置中
3. 自定義樣式是否在 `globals.css` 中
4. 是否需要清除快取

### Q: Firebase 連接失敗？

**A**: 檢查：
1. 環境變數是否正確設置
2. Firebase 專案是否啟用 Firestore
3. 網路連接是否正常
4. Firebase 配置是否正確

---

## 技術支援

如有問題或需要協助，請：
1. 查看本手冊的相關章節
2. 檢查專案的 GitHub Issues
3. 聯繫開發團隊

---

## 版本資訊

- **Next.js**: 15.5.0
- **React**: 19.1.0
- **TypeScript**: ^5
- **Tailwind CSS**: ^4
- **最後更新**: 2025年1月

---

**祝您使用愉快！** 🎉

